node_modules/
dist/
.env
.env.local
*.log

# Nenhum PEM no repositório. Cobre a chave privada do SFTP e também o bundle de
# CAs do RDS — este último é público, mas a AWS rotaciona as autoridades, e um
# bundle versionado envelhece sem ninguém notar até a conexão parar. Provisione
# no deploy: npm run baixar-ca-rds --workspace @infoprice/server
*.pem
var/
server/var/
web/dist/
*.tsbuildinfo
.DS_Store

# Bundle de design do Claude Design, mantido fora deste repositório de
# propósito: project/uploads/ contém um print de conversa com a senha do SFTP
# em texto puro, e este repositório é público. O bundle segue versionado no
# clone local, no commit de handoff.
project/
chats/
